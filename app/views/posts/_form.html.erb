<%= render "common/errors", obj: @post %>

<%= form.label :title %>
<%= form.text_field :tite, size: 20 %>

<%= form.label :body %>
<%= form.text_area :body, rows: 10, cols: 45 %>

<%= Post.human_attribute_name(:member_only) %>
<%= form.check_box :member_only %>
<%= form.label :member_only %>

