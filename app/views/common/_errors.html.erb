<!--objとして範囲を広めてる　◯：present?×：any?-->
<% if obj.errors.present? %>
  <div id ="errors">
    <h3>エラーがあります。</h3>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>